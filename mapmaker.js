import { CANVAS_WIDTH, CANVAS_HEIGHT } from "./modules/constants/graphicConstants.js";

const canvas = document.getElementById("mapmakerCanvas");
const context = canvas.getContext("2d");

const sprites = {
    
}